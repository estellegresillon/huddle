import styled from "styled-components";

const Bookmarks = () => <BookmarksWrapper>Bookmarks</BookmarksWrapper>;

export default Bookmarks;

const BookmarksWrapper = styled.div`
  align-items: center;
  display: flex;
  height: 100vh;
  width: calc(100% - 150px);
`;
