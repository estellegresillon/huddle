import styled from "styled-components";

const Chat = () => <ChatWrapper>Chat</ChatWrapper>;

export default Chat;

const ChatWrapper = styled.div`
  align-items: center;
  display: flex;
  height: 100vh;
  width: calc(100% - 150px);
`;
