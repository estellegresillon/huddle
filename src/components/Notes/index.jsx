import styled from "styled-components";

const Notes = () => <NotesWrapper>Notes</NotesWrapper>;

export default Notes;

const NotesWrapper = styled.div`
  align-items: center;
  display: flex;
  height: 100vh;
  width: calc(100% - 150px);
`;
